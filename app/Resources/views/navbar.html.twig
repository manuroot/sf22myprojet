{% block NAVIGATION_BAR %}
<div class="navbar navbar-fixed-top navbar-inverse">
    <div class="navbar-inner">
        <div class="container-fluid"> 
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> 
                <span class="icon-bar"></span>
                <span class="icon-bar"></span> 
                <span class="icon-bar"></span> 
            </a>
            <a class="brand" href="#">{% block toto %}PROJET(v1.0){% endblock %}</a>
            <a class="brand" href="{{ path('sonata_news_home') }}"> <i class="icon-book"></i> {{ 'Blog'|trans }}</a>
             <div class="btn-group pull-right"> 
            {% if not is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                     <a class="brand" href="{{ path('fos_user_registration_register') }}"> <i class="icon-user"></i> {{ 'Register'|trans }}</a>
                    
                    <a class="brand" href="{{ path('fos_user_security_login') }}"><i class="icon-user"></i> {{ 'Login'|trans }}</a>

                   {% else %}
                    <a class="btn dropdown-toggle" data-toggle="dropdown" href="#"> <i class="icon-user"></i>


                                        {% if is_granted("ROLE_USER") or is_granted("ROLE_USER") %}
                                        {{ app.user.username }} 
                                        {% else %}
                                                  Username
                                         {% endif %}
                        <span class="caret"></span> 
                    </a>
                                {% endif %}


                    <ul class="dropdown-menu">
                        <!--  <li class="divider"></li>-->

                                       {% if is_granted("ROLE_ADMIN") %}
                        <li><a href="{{ path('sonata_admin_dashboard') }}">{{ 'Admin Dashboard' }}</a>

                                       {% endif %}   

                        <li><a href="{{ path('sonata_user_profile_edit_authentication') }}">{{ 'Edit Profile' }}</a>
                        </li>

                                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                        <li><a href="{{ path('fos_user_change_password') }}">{{ 'Changer password' }}</a></li>
                                    {% else %}
                        <li>  <a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
                                    {% endif %}   


                    </ul>
                </div>
            
       
                <div class="nav-collapse">
                    <ul class="nav">

                        <li class="dropdown"> <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-white  icon-user"></i>
                                Section-1<b class="caret"></b> </a>
                            <ul class="dropdown-menu">
                                <li><a href=""><i class="icon-camera"></i> subsection-1</a></li>
                                <li><a href=""><i class="icon-camera"></i> subsection-2</a></li>

                            </ul>




                        </li>
                        <li class="dropdown"> <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class="icon-white  icon-user"></i>
                                Section-2<b class="caret"></b> </a>
                            <ul class="dropdown-menu">
                                <li><a href=""><i class="icon-camera"></i> subsection-1</a></li>
                                <li><a href=""><i class="icon-camera"></i> subsection-2</a></li>

                            </ul>




                        </li>



                    </ul>  

                </div>
                <!--/.nav-collapse -->
            </div>
        </div>
    </div>
{% endblock %}    