 {% if pager.haveToPaginate() %}


                                <div class="pagination pagination-centered">
                                   <ul>
                                        {% if pager.page != 1  and pager.page != pager.lastpage%}
                                             <li>
                     <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.previouspage }}"> {{ mything }} RECENTS</a> 
                                              
                                             </li>
                                        {% if pager.previouspage != 1 %}
                                          <li>          <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.previouspage }}">{{ pager.previouspage }}</a>
                                              </li>
                                             
                                             
                                        {% endif %}
                                        

                                        {# Set the number of pages to display in the pager #}
                                        {% for page in pager.getLinks() %}
                                            {% if page == pager.page %}
                                                <li class="active">
                                                    jhj  <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.page }}"> }}">{{ pager.page }}</a>
                                                </li>
                                            {% else %}
                                                <li> <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.previouspage }}">{{ pager.page }}</a></li>
                                            {% endif %}
                                        {% endfor %}
                                                
                                                 <li class="active">
                                                    <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.page }}">{{ pager.page }}</a>
                                                </li>

                                        {% if pager.nextpage != pager.lastpage %}
                                          <li>          <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.previouspage }}">{{ pager.nextpage }}</a>
                                              </li>
                                              
                                        {% endif %}
                                            
                                            <li>
                                                  <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.nextpage }}">+ {{ mything }} ANCIENS</a> 
                                             </li>
{% endif %}
                                             
                                               {% if pager.page == 1 %}
                                               <li class="active">
                                             <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.firstpage }}">{{ mything }} RECENTS</a> 
                                            </li>
                                             <li>
                                                 <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post) }) }}?page={{ pager.nextpage }}">{{ pager.nextpage }}
                                                 </a> 
                                            </li>
                                          <li>
                                             <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.lastpage }}">{{ mything }} ANCIENS</a> 
                                            </li>
                                            {% endif %}
                                            
                                            
                                            
                                              {% if pager.page == pager.lastpage %}
                                               <li>
                                             <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.firstpage }}">{{ mything }} RECENTS</a> 
                                            </li>
                                             <li>
                                                 <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.previouspage }}">{{ pager.previouspage }}
                                                 </a> 
                                            </li>
                                          <li class="active">
                                             <a href="{{ url('sonata_news_view', { 'permalink': sonata_news_permalink(post)}) }}?page={{ pager.lastpage}}">{{ mything }} ANCIENS</a> 
                                            </li>
                                            {% endif %}
                                    </ul>
                                    
                              
                                </div>
                      
                    {% endif %}
                        
