<div>
    <p>
{% for tag in alltags %}
    <span class="badge badge-warning">
    <!--Catégorie: -->
        <a href="{{ url('sonata_news_tag', {'tag': tag.slug} ) }}">{{ tag.name }}({{ tag.posts|length }})</a>
    </span>
{% endfor %}
</p>
</div>
