<div class="toggler">
<div id="effect" class="ui-widget-content ui-corner-all">
<h3 class="ui-widget-header ui-corner-all">Tags</h3>
<p>

{% for tag in alltags %}
    <!--<span class="badge badge-warning">-->
        <span class="weight-{{ tagweight[tag.name] }}">
    <!--Catégorie: -->
        <a href="{{ url('sonata_news_tag', {'tag': tag.slug} ) }}">{{ tag.name }}({{ tag.posts|length }})</a>
    </span>
{% endfor %}
    </p>
</div>
</div>

<a href="#" id="button" class="ui-state-default ui-corner-all">Tags</a>