{% extends "ApplicationSonataNewsBundle::layoutsimple.html.twig" %}

         
{% block javascripts %}
{{ parent() }}
{{ tinymce_init() }}
<script type="text/javascript">
          
$(document).ready(function() {
 $( "#application_sonata_eposttype_commentsCloseAt" ).datepicker({
maxDate: "+3M +10D",
changeMonth: true,
changeYeat: true,
numberOfMonths: 1,
dateFormat: "yy-mm-dd 18:00",
onClose: function( selectedDate ) {
$( "#form_bis" ).datepicker( "option", "minDate", selectedDate );
}
});
  
});






{% endblock %}
{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('bundles/applicationchangements/css/sliding.css') }}" type="text/css" media="all"/>
<link rel="stylesheet" href="{{ asset('select2/select2.css') }}" type="text/css" media="all"/>
<style>
        .monlabel {
            font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
            font-weight: bold;
        }
        .boxlist {
            border: 1px solid #D4E0EE;
            background-color: #f1f1f1;

        }

        .tinymce {
            border: 1px solid #D4E0EE;
            background-color: #f1f1f1;
            width:230px; height:100px;
            padding:20px;
            margin:10px;

        }
        table {
        width:100%;
    margin:auto;}
        td {
              padding:10px;
            margin:10px;
        }
      
        .myresume {
            border: 1px solid #D4E0EE;
         /*   background-color: #f1f1f1;*/
            width:330px; 
            height:300px;

        }
        .leserreurs {
            font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
            font-weight: bold;
            font-size:18px;
            color:red;
        }
        tr.single {
         background-color: #f1f1f1;
        }

    </style> 

{% endblock %}





{% block content_header '' %}
{% block title %}
POST: {{ entity.title }}
{% endblock %}
{% block content %}

<br><br>

<form action="{{ path('epost_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <table>
        <tbody>      
            <tr class="single"><td>
                    <div>
                  {{ form_row(edit_form.title) }}
                  {{ form_row(edit_form.author) }}
                  {{ form_row(edit_form.tags) }}
                  {{ form_row(edit_form.category) }}
                  {{ form_row(edit_form.author) }}


                 {#    {{ form_row(edit_form.services) }}#}
                        </div>
                      
          {#   {% if entity.image is not null %}               
                                      <p>Image Actuelle</p>
         <img width="150px" src="{{ vich_uploader_asset(entity, 'image') }}" alt="{{ entity.imageName }}" />
         {% else %}
           <p>Pas d'Image</p>
         {% endif %}
           </div>
            #}  
                    </td>
                    <td>
                        {{ form_row(edit_form.content) }}
{{ form_row(edit_form.abstract) }}

                        
                        </td>
                        <td>
                
             <img src="{% path entity.image, 'big' %}" width="100" />                
{{ form_rest(edit_form) }} 
                        
                        </td>
                    </tr>

                    <tr class="single"><td colspan="2">



                            <button type="submit" class="btn btn-small btn-warning">Modifier</button>

                            <a href="{{ path(btnretour)  }}" class="btn btn-small btn-success">Retour</a>
                            </form>       


                        </td></tr>
                </tbody>
            </table>



<br><br>



{% endblock %}

